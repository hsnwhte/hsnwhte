### Back-end Developer | Data Parsing & Normalization | FastAPI

10+ years in industrial operations — manufacturing planning, quality
management systems (ISO 9001 / AS9100), and ERP implementation — now
working full-time as a Python backend developer.

🔧 Currently building: [Fluxus](your-repo-link) — a generic, plugin-based
ETL/sync engine. Strategy-pattern architecture, protocol-based storage,
CLI tooling, 34+ automated tests.

What I focus on:

- Data Parsing & Normalization — turning inconsistent semi-structured
  sources (XML, JSON, HTML, CSV, API responses) into clean, validated,
  well-typed data, as pipelines with explicit extraction, transformation,
  and validation stages.
- Backend APIs — REST APIs with FastAPI or Django: clear endpoints,
  sensible error responses, documented request/response contracts.

📫 Open to freelance / contract work.
